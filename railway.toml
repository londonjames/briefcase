[build]
builder = "nixpacks"
buildCommand = "pip install -r backend/requirements.txt"

[deploy]
startCommand = "cd backend && gunicorn app:app --bind 0.0.0.0:$PORT --timeout 300 --workers 2 --threads 4 -k gthread"
watchPatterns = ["backend/**"]
